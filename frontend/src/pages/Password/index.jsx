import {  PasswordForm } from '@components';
import { Container } from './styles';

export const Password = () => {
  return (
    <Container>
      <PasswordForm>
        
      </PasswordForm>
    </Container>
  );
};
